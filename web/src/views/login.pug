doctype html
html
  head
    title Connexion Discord
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='./css/bootstrap.min.css')
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css")
    style.
      .discord-button {
        background-color: #5865F2;
        color: white;
        border: none;
        padding: 0.6rem 1.2rem;
        font-size: 1rem;
        font-weight: 600;
        border-radius: 0.375rem;
        cursor: pointer;
        transition: background-color 0.2s ease-in-out, transform 0.1s;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
      }

      .discord-button:hover {
        background-color: #4752c4;
      }

      .discord-button:active {
        transform: scale(0.97);
      }

  body(class='bg-light d-flex align-items-center justify-content-center vh-100')

    .card.shadow-lg(style='min-width: 350px;')
      .card-body.text-center
        h2.card-title.text-center.mb-4 Connexion
        p.text-muted Connecte-toi pour accéder à ton espace.

        a.discord-button(href=`https://discord.com/api/oauth2/authorize?client_id=${clientId}&redirect_uri=${encodeURIComponent(redirectUri)}&response_type=code&scope=identify`)
          i.fab.fa-discord.me-2
          | Se connecter avec Discord

